var cl = console.log;
// UNKNOWN
var userDetails;
var userName;
userDetails = 123;
userDetails = true;
userDetails = "Jhon";
//userDetails = userName
if (typeof userDetails === "string") {
    userName = userDetails;
    cl(userName);
}
